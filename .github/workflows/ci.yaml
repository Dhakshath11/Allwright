name: CI Pipeline

on:
  workflow_dispatch:
  
  push:
    branches:
      - main

  pull_request:

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print Hello
        run: echo "Workflow is running"